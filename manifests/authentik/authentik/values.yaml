authentik:
  secret_key: "changeme-local-only"
  postgresql:
    password: "changeme-local-only"

postgresql:
  enabled: true
  auth:
    password: "changeme-local-only"
    username: "authentik"
    database: "authentik"
  primary:
    persistence:
      enabled: true
      existingClaim: authentik-postgresql-nfs
