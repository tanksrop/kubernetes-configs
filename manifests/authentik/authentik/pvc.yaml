apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-postgresql-nfs
  namespace: authentik
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
