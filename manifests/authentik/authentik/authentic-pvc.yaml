apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-nfs
  namespace: authentik
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: shared-nfs-pv
  storageClassName: nfs-client
